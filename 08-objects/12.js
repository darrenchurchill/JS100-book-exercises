/**
 * Launch School Introduction to Programming with JavaScript
 * Ch. 8: Objects
 * Exercise 12
 *
 * Without creating a new function or changing the calls to `foo`, update the
 * code to fix the error and work as intended.
 */

function foo(bar) {
  console.log(bar, bar, bar);
}

foo("hello");  // should print "hello hello hello"
bar("hi");  // should print "hi hi hi"